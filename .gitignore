# OpenTofu/Terraform ignores
.terraform/             # Ignore the .terraform/ and .opentofu/ directories (plugins, modules cache)
.opentofu/

*.tfplan                # Ignore all plan files generated by 'tofu plan -out'
*.tfstate               # Ignore the main state file
*.tfstate.backup        # Ignore state backup files
.terraform.lock.hcl     # Ignore the provider lock file (can be regenerated by init)
.opentofu.lock.hcl      # Ignore the provider lock file (can be regenerated by init)

# Crash log files
crash.log

# VS Code specific ignores
.vscode/                # Ignore VS Code specific settings if you don't want to share them